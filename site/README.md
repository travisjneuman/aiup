# Site

Static files for **https://aiup.neuman.dev**.

Cloudflare Pages serves this folder (`site/`) as the project root. No build step. `catalog.json` is generated by `scripts/sync-public-docs` from `macos/aiup`.
